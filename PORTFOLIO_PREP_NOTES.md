# Portfolio Prep Notes

The site is ready for content updates, and the remaining work is mostly replacing placeholder copy in [index.html](C:/Users/Lant/Desktop/website/index.html).

Replace these template values first:

- `Portfolio Template` in the page title with your actual name or brand.
- `SecurePort` in the navbar logo with your preferred site name.
- `your.email@example.com` with your real email.
- `yourprofile` in the LinkedIn, GitHub, and Hack The Box links with your real handles.
- The hero headline, about text, stats, projects, certifications, blog posts, and timeline entries with your real experience.

Prep fixes already handled:

- Added the missing `Experience` nav link.
- Replaced fragile special-character labels with plain ASCII text for cleaner cross-environment rendering.
- Added `rel="noreferrer"` to external links that already open in new tabs.
